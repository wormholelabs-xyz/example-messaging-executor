/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS } from "../programs";

export const QUOTE_EXECUTION_DISCRIMINATOR = 1;

export function getQuoteExecutionDiscriminatorBytes() {
  return getU8Encoder().encode(QUOTE_EXECUTION_DISCRIMINATOR);
}

export type QuoteExecutionInstruction<
  TProgram extends string = typeof EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS,
  TAccountQuoterRegistration extends string | AccountMeta<string> = string,
  TAccountQuoterProgram extends string | AccountMeta<string> = string,
  TAccountQuoterConfig extends string | AccountMeta<string> = string,
  TAccountQuoterChainInfo extends string | AccountMeta<string> = string,
  TAccountQuoterQuoteBody extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountQuoterRegistration extends string
        ? ReadonlyAccount<TAccountQuoterRegistration>
        : TAccountQuoterRegistration,
      TAccountQuoterProgram extends string
        ? ReadonlyAccount<TAccountQuoterProgram>
        : TAccountQuoterProgram,
      TAccountQuoterConfig extends string
        ? ReadonlyAccount<TAccountQuoterConfig>
        : TAccountQuoterConfig,
      TAccountQuoterChainInfo extends string
        ? ReadonlyAccount<TAccountQuoterChainInfo>
        : TAccountQuoterChainInfo,
      TAccountQuoterQuoteBody extends string
        ? ReadonlyAccount<TAccountQuoterQuoteBody>
        : TAccountQuoterQuoteBody,
      ...TRemainingAccounts,
    ]
  >;

export type QuoteExecutionInstructionData = {
  discriminator: number;
  quoterAddress: ReadonlyUint8Array;
  cpiData: ReadonlyUint8Array;
};

export type QuoteExecutionInstructionDataArgs = {
  quoterAddress: ReadonlyUint8Array;
  cpiData: ReadonlyUint8Array;
};

export function getQuoteExecutionInstructionDataEncoder(): Encoder<QuoteExecutionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["quoterAddress", fixEncoderSize(getBytesEncoder(), 20)],
      ["cpiData", getBytesEncoder()],
    ]),
    (value) => ({ ...value, discriminator: QUOTE_EXECUTION_DISCRIMINATOR }),
  );
}

export function getQuoteExecutionInstructionDataDecoder(): Decoder<QuoteExecutionInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["quoterAddress", fixDecoderSize(getBytesDecoder(), 20)],
    ["cpiData", getBytesDecoder()],
  ]);
}

export function getQuoteExecutionInstructionDataCodec(): Codec<
  QuoteExecutionInstructionDataArgs,
  QuoteExecutionInstructionData
> {
  return combineCodec(
    getQuoteExecutionInstructionDataEncoder(),
    getQuoteExecutionInstructionDataDecoder(),
  );
}

export type QuoteExecutionInput<
  TAccountQuoterRegistration extends string = string,
  TAccountQuoterProgram extends string = string,
  TAccountQuoterConfig extends string = string,
  TAccountQuoterChainInfo extends string = string,
  TAccountQuoterQuoteBody extends string = string,
> = {
  /** QuoterRegistration PDA for the quoter */
  quoterRegistration: Address<TAccountQuoterRegistration>;
  /** Quoter implementation program */
  quoterProgram: Address<TAccountQuoterProgram>;
  /** Quoter config account (passed to quoter CPI) */
  quoterConfig: Address<TAccountQuoterConfig>;
  /** Quoter chain info account (passed to quoter CPI) */
  quoterChainInfo: Address<TAccountQuoterChainInfo>;
  /** Quoter quote body account (passed to quoter CPI) */
  quoterQuoteBody: Address<TAccountQuoterQuoteBody>;
  quoterAddress: QuoteExecutionInstructionDataArgs["quoterAddress"];
  cpiData: QuoteExecutionInstructionDataArgs["cpiData"];
};

export function getQuoteExecutionInstruction<
  TAccountQuoterRegistration extends string,
  TAccountQuoterProgram extends string,
  TAccountQuoterConfig extends string,
  TAccountQuoterChainInfo extends string,
  TAccountQuoterQuoteBody extends string,
  TProgramAddress extends Address =
    typeof EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS,
>(
  input: QuoteExecutionInput<
    TAccountQuoterRegistration,
    TAccountQuoterProgram,
    TAccountQuoterConfig,
    TAccountQuoterChainInfo,
    TAccountQuoterQuoteBody
  >,
  config?: { programAddress?: TProgramAddress },
): QuoteExecutionInstruction<
  TProgramAddress,
  TAccountQuoterRegistration,
  TAccountQuoterProgram,
  TAccountQuoterConfig,
  TAccountQuoterChainInfo,
  TAccountQuoterQuoteBody
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    quoterRegistration: {
      value: input.quoterRegistration ?? null,
      isWritable: false,
    },
    quoterProgram: { value: input.quoterProgram ?? null, isWritable: false },
    quoterConfig: { value: input.quoterConfig ?? null, isWritable: false },
    quoterChainInfo: {
      value: input.quoterChainInfo ?? null,
      isWritable: false,
    },
    quoterQuoteBody: {
      value: input.quoterQuoteBody ?? null,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("quoterRegistration", accounts.quoterRegistration),
      getAccountMeta("quoterProgram", accounts.quoterProgram),
      getAccountMeta("quoterConfig", accounts.quoterConfig),
      getAccountMeta("quoterChainInfo", accounts.quoterChainInfo),
      getAccountMeta("quoterQuoteBody", accounts.quoterQuoteBody),
    ],
    data: getQuoteExecutionInstructionDataEncoder().encode(
      args as QuoteExecutionInstructionDataArgs,
    ),
    programAddress,
  } as QuoteExecutionInstruction<
    TProgramAddress,
    TAccountQuoterRegistration,
    TAccountQuoterProgram,
    TAccountQuoterConfig,
    TAccountQuoterChainInfo,
    TAccountQuoterQuoteBody
  >);
}

export type ParsedQuoteExecutionInstruction<
  TProgram extends string = typeof EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** QuoterRegistration PDA for the quoter */
    quoterRegistration: TAccountMetas[0];
    /** Quoter implementation program */
    quoterProgram: TAccountMetas[1];
    /** Quoter config account (passed to quoter CPI) */
    quoterConfig: TAccountMetas[2];
    /** Quoter chain info account (passed to quoter CPI) */
    quoterChainInfo: TAccountMetas[3];
    /** Quoter quote body account (passed to quoter CPI) */
    quoterQuoteBody: TAccountMetas[4];
  };
  data: QuoteExecutionInstructionData;
};

export function parseQuoteExecutionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedQuoteExecutionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      quoterRegistration: getNextAccount(),
      quoterProgram: getNextAccount(),
      quoterConfig: getNextAccount(),
      quoterChainInfo: getNextAccount(),
      quoterQuoteBody: getNextAccount(),
    },
    data: getQuoteExecutionInstructionDataDecoder().decode(instruction.data),
  };
}
