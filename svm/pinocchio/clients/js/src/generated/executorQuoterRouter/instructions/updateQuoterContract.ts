/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_QUOTER_CONTRACT_DISCRIMINATOR = 0;

export function getUpdateQuoterContractDiscriminatorBytes() {
  return getU8Encoder().encode(UPDATE_QUOTER_CONTRACT_DISCRIMINATOR);
}

export type UpdateQuoterContractInstruction<
  TProgram extends string = typeof EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountQuoterRegistration extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSender extends string
        ? ReadonlySignerAccount<TAccountSender> &
            AccountSignerMeta<TAccountSender>
        : TAccountSender,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountQuoterRegistration extends string
        ? WritableAccount<TAccountQuoterRegistration>
        : TAccountQuoterRegistration,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateQuoterContractInstructionData = {
  discriminator: number;
  governanceMessage: ReadonlyUint8Array;
};

export type UpdateQuoterContractInstructionDataArgs = {
  governanceMessage: ReadonlyUint8Array;
};

export function getUpdateQuoterContractInstructionDataEncoder(): FixedSizeEncoder<UpdateQuoterContractInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", getU8Encoder()],
      ["governanceMessage", fixEncoderSize(getBytesEncoder(), 163)],
    ]),
    (value) => ({
      ...value,
      discriminator: UPDATE_QUOTER_CONTRACT_DISCRIMINATOR,
    }),
  );
}

export function getUpdateQuoterContractInstructionDataDecoder(): FixedSizeDecoder<UpdateQuoterContractInstructionData> {
  return getStructDecoder([
    ["discriminator", getU8Decoder()],
    ["governanceMessage", fixDecoderSize(getBytesDecoder(), 163)],
  ]);
}

export function getUpdateQuoterContractInstructionDataCodec(): FixedSizeCodec<
  UpdateQuoterContractInstructionDataArgs,
  UpdateQuoterContractInstructionData
> {
  return combineCodec(
    getUpdateQuoterContractInstructionDataEncoder(),
    getUpdateQuoterContractInstructionDataDecoder(),
  );
}

export type UpdateQuoterContractInput<
  TAccountPayer extends string = string,
  TAccountSender extends string = string,
  TAccountConfig extends string = string,
  TAccountQuoterRegistration extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Pays for account creation */
  payer: TransactionSigner<TAccountPayer>;
  /** Must match universal_sender_address in governance message */
  sender: TransactionSigner<TAccountSender>;
  /** Program config (reserved for integrator use) */
  config: Address<TAccountConfig>;
  /** QuoterRegistration PDA ['quoter_registration', quoter_address] */
  quoterRegistration: Address<TAccountQuoterRegistration>;
  /** System Program */
  systemProgram?: Address<TAccountSystemProgram>;
  governanceMessage: UpdateQuoterContractInstructionDataArgs["governanceMessage"];
};

export function getUpdateQuoterContractInstruction<
  TAccountPayer extends string,
  TAccountSender extends string,
  TAccountConfig extends string,
  TAccountQuoterRegistration extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends
    Address = typeof EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS,
>(
  input: UpdateQuoterContractInput<
    TAccountPayer,
    TAccountSender,
    TAccountConfig,
    TAccountQuoterRegistration,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateQuoterContractInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountSender,
  TAccountConfig,
  TAccountQuoterRegistration,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    sender: { value: input.sender ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    quoterRegistration: {
      value: input.quoterRegistration ?? null,
      isWritable: true,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("sender", accounts.sender),
      getAccountMeta("config", accounts.config),
      getAccountMeta("quoterRegistration", accounts.quoterRegistration),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getUpdateQuoterContractInstructionDataEncoder().encode(
      args as UpdateQuoterContractInstructionDataArgs,
    ),
    programAddress,
  } as UpdateQuoterContractInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountSender,
    TAccountConfig,
    TAccountQuoterRegistration,
    TAccountSystemProgram
  >);
}

export type ParsedUpdateQuoterContractInstruction<
  TProgram extends string = typeof EXECUTOR_QUOTER_ROUTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Pays for account creation */
    payer: TAccountMetas[0];
    /** Must match universal_sender_address in governance message */
    sender: TAccountMetas[1];
    /** Program config (reserved for integrator use) */
    config: TAccountMetas[2];
    /** QuoterRegistration PDA ['quoter_registration', quoter_address] */
    quoterRegistration: TAccountMetas[3];
    /** System Program */
    systemProgram: TAccountMetas[4];
  };
  data: UpdateQuoterContractInstructionData;
};

export function parseUpdateQuoterContractInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateQuoterContractInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 5,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      sender: getNextAccount(),
      config: getNextAccount(),
      quoterRegistration: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getUpdateQuoterContractInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
