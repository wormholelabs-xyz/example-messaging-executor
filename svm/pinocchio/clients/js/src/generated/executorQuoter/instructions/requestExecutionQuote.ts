/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { EXECUTOR_QUOTER_PROGRAM_ADDRESS } from "../programs";

export const REQUEST_EXECUTION_QUOTE_DISCRIMINATOR = new Uint8Array([
  3, 0, 0, 0, 0, 0, 0, 0,
]);

export function getRequestExecutionQuoteDiscriminatorBytes() {
  return getBytesEncoder().encode(REQUEST_EXECUTION_QUOTE_DISCRIMINATOR);
}

export type RequestExecutionQuoteInstruction<
  TProgram extends string = typeof EXECUTOR_QUOTER_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountChainInfo extends string | AccountMeta<string> = string,
  TAccountQuoteBody extends string | AccountMeta<string> = string,
  TAccountEventCpi extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountChainInfo extends string
        ? ReadonlyAccount<TAccountChainInfo>
        : TAccountChainInfo,
      TAccountQuoteBody extends string
        ? ReadonlyAccount<TAccountQuoteBody>
        : TAccountQuoteBody,
      TAccountEventCpi extends string
        ? ReadonlyAccount<TAccountEventCpi>
        : TAccountEventCpi,
      ...TRemainingAccounts,
    ]
  >;

export type RequestExecutionQuoteInstructionData = {
  dstChain: number;
  dstAddr: ReadonlyUint8Array;
  refundAddr: ReadonlyUint8Array;
  requestBytes: ReadonlyUint8Array;
  relayInstructions: ReadonlyUint8Array;
};

export type RequestExecutionQuoteInstructionDataArgs =
  RequestExecutionQuoteInstructionData;

export function getRequestExecutionQuoteInstructionDataEncoder(): Encoder<RequestExecutionQuoteInstructionDataArgs> {
  return getStructEncoder([
    ["dstChain", getU16Encoder()],
    ["dstAddr", fixEncoderSize(getBytesEncoder(), 32)],
    ["refundAddr", fixEncoderSize(getBytesEncoder(), 32)],
    ["requestBytes", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    [
      "relayInstructions",
      addEncoderSizePrefix(getBytesEncoder(), getU32Encoder()),
    ],
  ]);
}

export function getRequestExecutionQuoteInstructionDataDecoder(): Decoder<RequestExecutionQuoteInstructionData> {
  return getStructDecoder([
    ["dstChain", getU16Decoder()],
    ["dstAddr", fixDecoderSize(getBytesDecoder(), 32)],
    ["refundAddr", fixDecoderSize(getBytesDecoder(), 32)],
    ["requestBytes", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    [
      "relayInstructions",
      addDecoderSizePrefix(getBytesDecoder(), getU32Decoder()),
    ],
  ]);
}

export function getRequestExecutionQuoteInstructionDataCodec(): Codec<
  RequestExecutionQuoteInstructionDataArgs,
  RequestExecutionQuoteInstructionData
> {
  return combineCodec(
    getRequestExecutionQuoteInstructionDataEncoder(),
    getRequestExecutionQuoteInstructionDataDecoder(),
  );
}

export type RequestExecutionQuoteInput<
  TAccountConfig extends string = string,
  TAccountChainInfo extends string = string,
  TAccountQuoteBody extends string = string,
  TAccountEventCpi extends string = string,
> = {
  /** Program config (reserved for CPI compatibility) */
  config: Address<TAccountConfig>;
  /** ChainInfo PDA for destination chain */
  chainInfo: Address<TAccountChainInfo>;
  /** QuoteBody PDA for destination chain */
  quoteBody: Address<TAccountQuoteBody>;
  /** Event CPI account (reserved for CPI compatibility) */
  eventCpi: Address<TAccountEventCpi>;
  dstChain: RequestExecutionQuoteInstructionDataArgs["dstChain"];
  dstAddr: RequestExecutionQuoteInstructionDataArgs["dstAddr"];
  refundAddr: RequestExecutionQuoteInstructionDataArgs["refundAddr"];
  requestBytes: RequestExecutionQuoteInstructionDataArgs["requestBytes"];
  relayInstructions: RequestExecutionQuoteInstructionDataArgs["relayInstructions"];
};

export function getRequestExecutionQuoteInstruction<
  TAccountConfig extends string,
  TAccountChainInfo extends string,
  TAccountQuoteBody extends string,
  TAccountEventCpi extends string,
  TProgramAddress extends Address = typeof EXECUTOR_QUOTER_PROGRAM_ADDRESS,
>(
  input: RequestExecutionQuoteInput<
    TAccountConfig,
    TAccountChainInfo,
    TAccountQuoteBody,
    TAccountEventCpi
  >,
  config?: { programAddress?: TProgramAddress },
): RequestExecutionQuoteInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountChainInfo,
  TAccountQuoteBody,
  TAccountEventCpi
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? EXECUTOR_QUOTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    chainInfo: { value: input.chainInfo ?? null, isWritable: false },
    quoteBody: { value: input.quoteBody ?? null, isWritable: false },
    eventCpi: { value: input.eventCpi ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("chainInfo", accounts.chainInfo),
      getAccountMeta("quoteBody", accounts.quoteBody),
      getAccountMeta("eventCpi", accounts.eventCpi),
    ],
    data: getRequestExecutionQuoteInstructionDataEncoder().encode(
      args as RequestExecutionQuoteInstructionDataArgs,
    ),
    programAddress,
  } as RequestExecutionQuoteInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountChainInfo,
    TAccountQuoteBody,
    TAccountEventCpi
  >);
}

export type ParsedRequestExecutionQuoteInstruction<
  TProgram extends string = typeof EXECUTOR_QUOTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Program config (reserved for CPI compatibility) */
    config: TAccountMetas[0];
    /** ChainInfo PDA for destination chain */
    chainInfo: TAccountMetas[1];
    /** QuoteBody PDA for destination chain */
    quoteBody: TAccountMetas[2];
    /** Event CPI account (reserved for CPI compatibility) */
    eventCpi: TAccountMetas[3];
  };
  data: RequestExecutionQuoteInstructionData;
};

export function parseRequestExecutionQuoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestExecutionQuoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      chainInfo: getNextAccount(),
      quoteBody: getNextAccount(),
      eventCpi: getNextAccount(),
    },
    data: getRequestExecutionQuoteInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
