/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU16Decoder,
  getU16Encoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  type AccountMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { EXECUTOR_QUOTER_PROGRAM_ADDRESS } from "../programs";

export const REQUEST_QUOTE_DISCRIMINATOR = new Uint8Array([
  2, 0, 0, 0, 0, 0, 0, 0,
]);

export function getRequestQuoteDiscriminatorBytes() {
  return getBytesEncoder().encode(REQUEST_QUOTE_DISCRIMINATOR);
}

export type RequestQuoteInstruction<
  TProgram extends string = typeof EXECUTOR_QUOTER_PROGRAM_ADDRESS,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountChainInfo extends string | AccountMeta<string> = string,
  TAccountQuoteBody extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountChainInfo extends string
        ? ReadonlyAccount<TAccountChainInfo>
        : TAccountChainInfo,
      TAccountQuoteBody extends string
        ? ReadonlyAccount<TAccountQuoteBody>
        : TAccountQuoteBody,
      ...TRemainingAccounts,
    ]
  >;

export type RequestQuoteInstructionData = {
  dstChain: number;
  dstAddr: ReadonlyUint8Array;
  refundAddr: ReadonlyUint8Array;
  requestBytes: ReadonlyUint8Array;
  relayInstructions: ReadonlyUint8Array;
};

export type RequestQuoteInstructionDataArgs = RequestQuoteInstructionData;

export function getRequestQuoteInstructionDataEncoder(): Encoder<RequestQuoteInstructionDataArgs> {
  return getStructEncoder([
    ["dstChain", getU16Encoder()],
    ["dstAddr", fixEncoderSize(getBytesEncoder(), 32)],
    ["refundAddr", fixEncoderSize(getBytesEncoder(), 32)],
    ["requestBytes", addEncoderSizePrefix(getBytesEncoder(), getU32Encoder())],
    [
      "relayInstructions",
      addEncoderSizePrefix(getBytesEncoder(), getU32Encoder()),
    ],
  ]);
}

export function getRequestQuoteInstructionDataDecoder(): Decoder<RequestQuoteInstructionData> {
  return getStructDecoder([
    ["dstChain", getU16Decoder()],
    ["dstAddr", fixDecoderSize(getBytesDecoder(), 32)],
    ["refundAddr", fixDecoderSize(getBytesDecoder(), 32)],
    ["requestBytes", addDecoderSizePrefix(getBytesDecoder(), getU32Decoder())],
    [
      "relayInstructions",
      addDecoderSizePrefix(getBytesDecoder(), getU32Decoder()),
    ],
  ]);
}

export function getRequestQuoteInstructionDataCodec(): Codec<
  RequestQuoteInstructionDataArgs,
  RequestQuoteInstructionData
> {
  return combineCodec(
    getRequestQuoteInstructionDataEncoder(),
    getRequestQuoteInstructionDataDecoder(),
  );
}

export type RequestQuoteInput<
  TAccountConfig extends string = string,
  TAccountChainInfo extends string = string,
  TAccountQuoteBody extends string = string,
> = {
  /** Program config (reserved for CPI compatibility) */
  config: Address<TAccountConfig>;
  /** ChainInfo PDA for destination chain */
  chainInfo: Address<TAccountChainInfo>;
  /** QuoteBody PDA for destination chain */
  quoteBody: Address<TAccountQuoteBody>;
  dstChain: RequestQuoteInstructionDataArgs["dstChain"];
  dstAddr: RequestQuoteInstructionDataArgs["dstAddr"];
  refundAddr: RequestQuoteInstructionDataArgs["refundAddr"];
  requestBytes: RequestQuoteInstructionDataArgs["requestBytes"];
  relayInstructions: RequestQuoteInstructionDataArgs["relayInstructions"];
};

export function getRequestQuoteInstruction<
  TAccountConfig extends string,
  TAccountChainInfo extends string,
  TAccountQuoteBody extends string,
  TProgramAddress extends Address = typeof EXECUTOR_QUOTER_PROGRAM_ADDRESS,
>(
  input: RequestQuoteInput<
    TAccountConfig,
    TAccountChainInfo,
    TAccountQuoteBody
  >,
  config?: { programAddress?: TProgramAddress },
): RequestQuoteInstruction<
  TProgramAddress,
  TAccountConfig,
  TAccountChainInfo,
  TAccountQuoteBody
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? EXECUTOR_QUOTER_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    config: { value: input.config ?? null, isWritable: false },
    chainInfo: { value: input.chainInfo ?? null, isWritable: false },
    quoteBody: { value: input.quoteBody ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("config", accounts.config),
      getAccountMeta("chainInfo", accounts.chainInfo),
      getAccountMeta("quoteBody", accounts.quoteBody),
    ],
    data: getRequestQuoteInstructionDataEncoder().encode(
      args as RequestQuoteInstructionDataArgs,
    ),
    programAddress,
  } as RequestQuoteInstruction<
    TProgramAddress,
    TAccountConfig,
    TAccountChainInfo,
    TAccountQuoteBody
  >);
}

export type ParsedRequestQuoteInstruction<
  TProgram extends string = typeof EXECUTOR_QUOTER_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Program config (reserved for CPI compatibility) */
    config: TAccountMetas[0];
    /** ChainInfo PDA for destination chain */
    chainInfo: TAccountMetas[1];
    /** QuoteBody PDA for destination chain */
    quoteBody: TAccountMetas[2];
  };
  data: RequestQuoteInstructionData;
};

export function parseRequestQuoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRequestQuoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      config: getNextAccount(),
      chainInfo: getNextAccount(),
      quoteBody: getNextAccount(),
    },
    data: getRequestQuoteInstructionDataDecoder().decode(instruction.data),
  };
}
